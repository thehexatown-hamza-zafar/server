import express from "express";

const app = express();

const PORT = 3001;
//  server instance listening at port....
//  server instance listening at port....

// app.listen(PORT, () => {
//   console.log(`App listening on PORT: ${PORT}`);
// });


app.listen(PORT, () => {
    console.log(`App listening on PORT: ${PORT}`);
  });
  

